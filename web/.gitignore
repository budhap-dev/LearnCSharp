node_modules
dist
.astro
*.local
.DS_Store

# Generated by `npm run capture` from the .NET lessons project - never edit by hand,
# and never commit: it is rebuilt on every build so it cannot drift.
public/data/
